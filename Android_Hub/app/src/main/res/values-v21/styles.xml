<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Main view styles -->
    <style name="Main" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize the titlebars -->
        <item name="android:colorPrimary">@color/colorPrimary</item>

        <!-- Customize the statusbar -->
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>

        <!-- Customize the accent of the buttons, checkboxes, etc -->
        <item name="android:colorAccent">@color/colorPrimary</item>
    </style>


    <!-- Setup view styles -->
    <style name="Setup" parent="android:Theme.Material.NoActionBar">
        <!-- Customize the statusbar -->
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>

        <!-- Customize the accent of the buttons, checkboxes, etc -->
        <item name="android:colorAccent">@color/colorPrimary</item>
    </style>

    <!-- Hub view styles -->
    <style name="Hub" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize the statusbar -->
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>

        <!-- Customize the recents view -->
        <item name="colorPrimary">@color/colorPrimaryDark</item>

        <!-- Customize the accent of the buttons, checkboxes, etc -->
        <item name="android:colorAccent">@color/colorPrimary</item>

        <item name="colorAccent">@color/accent</item>

        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>

        <item name="actionBarStyle">@style/ThemeOverlay.AppCompat.ActionBar</item>

    </style>

    <!-- Theme for the next button on the Setup view -->
    <style name="Next">
        <item name="android:paddingEnd">30dp</item>
        <item name="android:gravity">center_vertical|end</item>
        <item name="android:drawableRight">@drawable/ic_next_enable</item>
    </style>


    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
    </style>

</resources>
